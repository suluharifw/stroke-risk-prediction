# Core (runtime + training)
pandas>=2.0
numpy>=1.24
scikit-learn==1.3.2          # pinned: artifacts were trained on 1.2/1.3
joblib>=1.3
imbalanced-learn>=0.11       # BorderlineSMOTE, RandomUnderSampler, SMOTEENN

# Serving
Flask>=3.0
flask-cors>=4.0

# Pipeline / notebooks-as-scripts (EDA + evaluation figures)
matplotlib>=3.7
seaborn>=0.12
pyarrow>=14.0                # parquet output for the feature matrix

# Dev / tests
pytest>=7.4
