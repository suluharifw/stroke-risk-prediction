# Reports

`figures/` holds plots generated by the pipeline:

- `01_class_distribution.png`, `02_continuous_distributions.png` — from stage 01 (EDA)
- `cm_<model>.png` — confusion matrices from stage 05 (evaluation)

These are regenerable and excluded from version control.
